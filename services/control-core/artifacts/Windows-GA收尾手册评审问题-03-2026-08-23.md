# Windows GA 收尾手册评审问题 03

- 日期：2026 年 8 月 23 日
- 发布定位：仅供本人使用，Personal/Internal GA
- Git 权限：全部 Gate 通过并再次确认后，允许 commit、tag、push；禁止 force push

## 已核实情况

远端 `origin/main @ 389e43c` 已包含：

- `tools/release/sign_release.ps1`
- `tools/updater/keygen.sh`
- `tools/updater/make_latest_json.py`
- `tools/license-policy/`
- ADR-0023

但以下内容属于长期私密凭据：

1. 自签 Authenticode 证书的私钥。
2. Tauri Updater 私钥和私钥密码。

Updater 私钥一旦丢失，已经安装的客户端将拒绝以后使用另一把密钥签发的更新；私钥或密码一旦写入仓库、artifact、日志或对话，则必须废弃并重新规划迁移。

## 唯一确认问题

私钥生成和保管采用哪种方式？

推荐：**Zcode 只准备命令、校验公钥和修改配置；执行到生成私钥时暂停，由你亲自在本机输入密码并完成两个离线备份。Zcode 不读取、不打印、不复制、不写入报告或仓库。**

请确认是否采用这个推荐方式。
