# P3.9 下一步确认

## 当前已完成

- 修复了 `CreateRestrictedToken` 的初始化错误。
- 受限令牌创建、属性验证和句柄释放测试已通过。
- 3.1 手工命令已经可以完成初始化检查。

## 尚未完成的核心工作

当前 `WindowsProcessSandbox` 还没有使用 broker 创建的受限令牌启动实际 runner；
Workspace ACL 目前是配置探测，不是系统 ACL 改写；网络 egress 目前是策略判断，
不是操作系统级阻断。

## 待确认问题

你希望下一步是哪一种？

1. **继续由我直接实现完整 P3.9**：把受限令牌接入真实 runner 启动，再实现实际 Workspace ACL 和网络隔离，并补真机对抗测试。
2. **先完成当前 3.1 人工验收**：你在 PowerShell 运行已修复命令，确认输出后，我再继续下一项。
