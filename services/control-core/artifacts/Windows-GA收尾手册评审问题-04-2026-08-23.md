# Windows GA 收尾手册评审问题 04

- 日期：2026 年 8 月 23 日
- 发布定位：仅供本人使用，Personal/Internal GA
- 私钥处理：由用户本人输入密码并离线备份，Zcode 不接触私钥内容

## 已核实情况

远端 `origin/main @ 389e43c` 中存在：

- `tools/updater/README.md`
- `tools/updater/keygen.sh`
- `tools/updater/make_latest_json.py`

但不存在实际客户端 Updater 集成：

- 没有 Rust `tauri-plugin-updater` 依赖。
- 没有前端 `@tauri-apps/plugin-updater` 依赖。
- `tauri.conf.json` 没有 `plugins.updater` 配置。
- 没有检查、下载、安装、渠道读取、降级授权或中断恢复实现。

因此，原手册第 4 节不能直接进行“2-3 小时演练”。若保留为硬 Gate，必须先增加一个完整 Updater 实现阶段，并重新估算工期。

## 唯一确认问题

Personal/Internal GA 是否必须现在包含真正的自动更新和 N-1 自动回退？

推荐：**暂不作为个人 GA 的硬 Gate。当前采用版本化安装包、发布哈希、数据库备份和手工安装 N-1 包回退；把自动更新列为 GA 后的独立 P5 工作。**

请确认采用推荐方案，还是要求 Zcode 在本次任务中完整实现 Updater。
