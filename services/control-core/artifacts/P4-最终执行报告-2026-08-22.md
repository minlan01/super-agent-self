# P4 最终执行报告

- 日期：2026 年 8 月 22 日
- 项目：`D:\agent\Agents\super-agent-self`
- 手册：`D:\P4-真Sidecar与IPC契约实现手册.md`
- 执行约束：不执行 `commit`、`push`、`reset`、`clean`
- 当前工作树：未提交的修改和未跟踪产物仍保留
- 最终结论：**NO-GO（可继续工程验证，不可标记 Windows v1.0 GA）**

## 1. 本轮完成内容

1. 核对 P4 手册、`docs/releases/v1.0-gate.md`、源码和现有证据目录。
2. 修正 Gate 文档中的证据引用：旧的 `packaged-api-smoke.json` 为 `null`，已不再作为依据。
3. 使用当前打包资源重新执行真实 API smoke，并保留脱敏证据。
4. 清理旧测试进程/临时目录后，重新执行 IPC 单测与 Windows 管道集成测试。
5. 重新确认桌面前端构建、Rust 检查、Python 编译和 diff 空白检查。
6. 尝试独立验证 1.0.0 NSIS 冷安装启动与静默卸载，并把无法稳定复现的结果标为待处理，而不是 PASS。

## 2. PASS

### 2.1 IPC 与 Sidecar

| 项目 | 结果 | 证据 |
|---|---:|---|
| IPC 契约单测 | `15 passed` | `services/control-core/artifacts/p4-final-release-suite-2026-08-22-rerun/ipc-contract-unit.txt` |
| Windows Named Pipe 集成 | `6 passed` | `services/control-core/artifacts/p4-final-release-suite-2026-08-22-rerun/ipc-pipe-integration.txt` |
| 合并复跑 | `21 passed`，退出码 `0` | `services/control-core/artifacts/p4-combined-rerun-console-2026-08-22.txt`、`p4-combined-rerun-exit-2026-08-22.txt` |
| PowerShell IPC | `100/100`，p95 `15.825ms` | `services/control-core/artifacts/p4-final-release-suite-2026-08-22-rerun/ipc-powershell.json` |
| 第二账户 DACL | Win32 `error 5`，通过 | `services/control-core/artifacts/p4-second-account-dacl-2026-08-22/second-account-dacl.json` |
| 生命周期 | 强制清理、正常关闭、3 次重启、第 4 次熔断通过 | `services/control-core/artifacts/p4-final-release-suite-2026-08-22-rerun/installed-desktop-lifecycle.json` |

合并复跑首次出现的 `WinError 32` 是旧 `.pytest_tmp` 中残留 sidecar 持有 SQLite 文件造成的 fixture 清理问题；确认无存活 sidecar 后重新运行，结果为 `21 passed`。该问题应作为测试环境清理前置条件保留，不应解释为 IPC 协议失败。

### 2.2 真实打包 API smoke

使用当前 `apps/desktop/src-tauri/resources/sidecar/sidecar.exe` 执行：

- hello/版本协商：通过
- ping：通过
- 未授权健康请求：返回 `401`
- 注册：`201`
- 登录：`200`
- 创建任务：`201`
- 任务列表：`200`
- 审批列表：`200`
- `ipc.shutdown`：通过

脱敏证据：
`services/control-core/artifacts/p4-packaged-api-smoke-2026-08-22.json`

运行时 Bearer token、生成的用户名和任务标识均未写入证据。

### 2.3 构建与静态检查

- `npm.cmd run build`：通过。
- `cargo check`：通过。
- `python -m compileall`：通过。
- `git diff --check`：通过。

当前产物：

- Installer：`apps/desktop/src-tauri/target/release/bundle/nsis/Zcode Desktop Agent_1.0.0_x64-setup.exe`
- Installer SHA-256：`6138980F1040326E41A8763BBC89E04215F52DA9B145F4A2B03AA54B1D763346`
- Desktop SHA-256：`6A89D49EA3A9D60EEA5C4CD3F5E203B103924B61DFE9B69003F700A410C12ACB`
- Sidecar SHA-256：`F533F91F79CBCBE839F496CC7EC11638C37F21C41242A7E84A0222C265686B8D`
- Sidecar：91 个文件，约 `119.576 MiB`

## 3. PENDING

1. 手工 UI 验收：登录、任务创建、执行、取消、审批点击和时间线确认。
2. 干净 Windows 11 标准用户、第二账户和 Windows 10 22H2 矩阵。
3. 当前 1.0.0 安装包在冷数据库/冷主机条件下的可重复启动与静默卸载证据。
4. 在独立构建主机或 CI 上从同一源码和锁文件重建并比对摘要。
5. Release approver、N-1 安装包摘要和分阶段发布授权。

### 3.1 1.0.0 安装复核的实际结果

独立脚本 `apps/desktop/tests/test_installed_release_1_0_0.ps1` 的安装阶段退出码为 `0`，能找到 `tauri-spike.exe`、`sidecar.exe` 和卸载器；但 `measure_startup.ps1` 在 15 秒内没有观察到 `control-core sidecar ready`，脚本退出码为 `1`。随后临时目录清理成功。

证据：

- `services/control-core/artifacts/p4-installed-1.0.0-console-2026-08-22.txt`
- `services/control-core/artifacts/p4-installed-1.0.0-exit-2026-08-22.txt`

因此，旧的 1.0.0 启动数字（`2.755 / 1.662 / 1.967s`）只能作为此前套件的历史证据，不能替代本轮冷安装可重复性复核。

## 4. BLOCKED

1. 生产 Authenticode 证书、时间戳和所有 exe/dll 的签名。
2. 签名更新器、渠道元数据、中断恢复和 N-1 原子回滚。
3. 离线 WebView2 运行时与离线安装路径。
4. Node/Rust 许可证策略和审批记录。
5. 不可变 commit/tag：用户明确要求不执行 commit，因此该项保持 BLOCKED。

自签名复制/篡改测试只能证明篡改检测路径，不能满足生产签名：
`services/control-core/artifacts/p4-final-authenticode.json`

## 5. 证据一致性修正

- `services/control-core/artifacts/p4-final-release-suite-2026-08-22-rerun/packaged-api-smoke.json` 内容为 `null`，已从 Gate 证据链移除。
- 新的脱敏 API smoke 证据为 `p4-packaged-api-smoke-2026-08-22.json`。
- `p4-final-install.json` 与 `p4-final-uninstall.json` 指向旧 `0.1.0` 安装器，不能标记为当前 1.0.0 证据。
- Gate 文档已同步注明上述边界和 1.0.0 冷安装复核待处理状态。

## 6. 重新开放发布前的最小顺序

```powershell
# PowerShell，项目根目录
cd D:\agent\Agents\super-agent-self

# 1. 清理并确认无残留 sidecar 后，先单独运行两组测试
& .\services\control-core\.venv-win\Scripts\python.exe -m pytest .\services\control-core\tests\unit\test_ipc_contract.py -q
& .\services\control-core\.venv-win\Scripts\python.exe -m pytest .\services\control-core\tests\integration\test_sidecar_pipe_windows.py -q

# 2. 重新验证当前 1.0.0 安装包的冷启动、UI 和静默卸载
& powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\apps\desktop\tests\test_installed_release_1_0_0.ps1

# 3. 完成手工 UI 和干净系统矩阵后，再更新 Gate 决策
Get-Content .\docs\releases\v1.0-gate.md
```

## 7. 最终判定

**NO-GO。** P4 的真实 Sidecar、IPC v1、DACL、API 代理、生命周期和当前主机自动化证据已基本完成；但手工 UI、冷安装可重复性、干净系统矩阵、生产签名、更新回滚、离线 WebView2、许可证策略和不可变发布身份仍未闭合。当前产物适合继续工程验证，不适合对外发布或标记为 GA。
