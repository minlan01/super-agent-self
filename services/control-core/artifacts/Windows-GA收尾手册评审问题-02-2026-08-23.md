# Windows GA 收尾手册评审问题 02

- 日期：2026 年 8 月 23 日
- 待评审手册：`D:\Windows-GA收尾执行手册.md`
- 发布定位：仅供本人使用，建议命名为 **Personal/Internal GA**

## 当前 Git 事实

- 本地分支：`main`
- 本地 HEAD：`c9a4796`
- 本地跟踪的 `origin/main`：`9fc774d`
- 当前显示：本地比 `origin/main` 超前 1 个提交
- 手册声称目标基线：`389e43c`

因此，手册中的直接 `git pull` 不能原样交给 Zcode。正确前置步骤应为：

```powershell
git status --short --branch
git fetch origin --prune
git log --oneline --decorate --graph --max-count=20 --all
git rev-list --left-right --count HEAD...origin/main
```

在确认本地 `c9a4796`、远端实际 HEAD 和手册所称 `389e43c` 的关系前，禁止合并、变基或覆盖。

## 唯一确认问题

是否授权 Zcode 在所有 Gate 实际通过后执行以下 Git 发布操作：

```text
git add / git commit / git tag / git push
```

推荐答案：**允许，但必须满足全部 Gate、先展示待提交文件和 diff、禁止 force push，并在执行 commit/tag/push 前再次停下来让你确认。**
