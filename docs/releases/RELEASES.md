# Zcode Desktop Agent — 版本发布指纹

> 每次发布在此登记。安装者应核对本页 SHA-256 后再运行(自签期唯一
> 的完整性渠道;升级生产证书后签名验证优先,指纹保留作冗余)。

## v1.0.0(候选)

| 产物 | SHA-256 | 大小 |
|---|---|---|
| 安装器(NSIS) | _(发布时填写)_ | _(填写)_ |
| tauri-spike.exe | _(发布时填写)_ | |
| sidecar.exe | _(发布时填写)_ | |

- 构建来源: commit _(tag v1.0.0 指向的 hash)_
- 云端可复现构建 digest(GitHub Actions `release-digests-*` 工件): _(填写)_
- 签名姿态: 自签(ADR-0023);证书指纹: _(发布时填写)_
- 更新公钥(minisign): _(首次生成后填写)_

<!--
登记规范:
1. tag 打完后,本地与 CI 两处 digest 一致才允许登记
2. 自签期额外登记自签证书的 SHA-1 指纹(signtool 显示)
3. 对外分发启动时,本文件顶部加一行横幅注明已切换生产证书
-->
